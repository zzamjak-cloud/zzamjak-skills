---
name: blender-addon-release-workflow
description: "Blender 애드온/Extension 소스(blender_manifest.toml, bpy 등록·연산자) 또는 개발·테스트·패키징·릴리스·GitHub Pages 업데이트 배포를 변경하거나 검증할 때 사용한다. 씬 편집, 모델링, 머티리얼·렌더 결과 변경 같은 일반 Blender 작업에는 사용하지 않는다."
---

# Blender 애드온 개발·배포

개발 반복과 배포를 분리한다. 기능 수정 중에는 프로젝트 전용 Blender 프로필에서 로컬 소스를 직접 로드해 검증하고, 사용자가 릴리스나 배포를 요청했을 때만 GitHub 릴리스와 원격 Extension 저장소를 갱신한다.

## 불변 조건

- 작업 전 대상 프로젝트의 Git 상태와 원격 동기화를 먼저 확인한다.
- 개발 테스트는 일상적으로 사용하는 Blender 프로필과 설치된 릴리스 버전을 건드리지 않는다.
- Blender 애드온 개발 환경을 새로 만들거나 정비할 때 macOS용 `scripts/dev_run.sh`와 Windows용 `scripts/dev_run.ps1`·`scripts/dev_run.bat`을 항상 함께 제공한다. 현재 작업 환경이 한 운영체제뿐이어도 다른 운영체제 실행기를 생략하지 않는다.
- 프로젝트마다 고유한 프로필 디렉터리와 매니페스트 `id`를 사용한다.
- 개발 프로필은 저장소 소스를 `extensions/user_default/<addon-id>`로 연결하며, 빌드 ZIP 설치를 반복하지 않는다.
- GitHub 소유자는 별도 지시가 없으면 `zzamjak-cloud`, 공개 저장소 이름은 충돌을 피할 수 있는 구체적인 `<Project>-Blender` 형식을 우선 검토한다.
- 공개 저장소 생성, push, tag, release 등 외부 쓰기는 현재 요청이 명시적으로 승인한 범위에서만 실행한다.
- 기존 원격 태그를 재사용하거나 강제 갱신하지 않는다.
- “자동 업데이트”는 시작 시 자동 확인과 알림을 뜻하며, 사용자 승인 없는 무인 설치라고 설명하지 않는다.

## 작업 라우팅

- 기능 구현·수정·로컬 테스트라면 [개발 프로필](references/dev-profile.md)을 읽고 macOS와 Windows 실행기 세트를 확인한 뒤 현재 운영체제의 격리 프로필에서 검증한다. 릴리스는 만들지 않는다.
- 새 애드온의 공개 배포 기반을 만들거나 릴리스·자동 업데이트가 요청되면 [GitHub Extension 배포](references/github-extension-distribution.md)도 읽고 순서대로 진행한다.
- 기존 설치본과 과거 스크립트를 정리하는 요청이면 현재 Blender 활성 설치본, 매니페스트, 등록 연산자와 원본 파일을 비교해 최신 동작만 남긴 뒤 위 흐름에 연결한다.
- “나중에”, “향후”, “배포하고 싶다”, “준비해 달라” 같은 미래·희망 표현은 로컬 배포 기반 준비만 승인한다. 공개 저장소 생성, push, tag, Release, Pages 배포는 현재 실행을 명시적으로 요청한 경우에만 수행한다.

## 완료 기준

- 정적 테스트뿐 아니라 격리된 실제 Blender 프로세스에서 등록·핵심 기능·해제를 검증한다.
- macOS와 Windows 개발 실행기 및 양쪽 사용법이 모두 존재하는지 회귀 검사한다. 현재 운영체제가 아닌 쪽은 구문·인코딩·인자 전달·안전 처리까지 정적으로 검증하고 실제 런타임 미검증 사실을 보고한다.
- 배포 요청이 포함된 경우 Git 커밋/태그, Actions, Release 자산, Pages `index.json`, 원격 설치 또는 업데이트 결과를 각각 확인한다.
- README에는 개발 프로필 실행법과 사용자용 원격 저장소 URL을 구분해 기록한다.
